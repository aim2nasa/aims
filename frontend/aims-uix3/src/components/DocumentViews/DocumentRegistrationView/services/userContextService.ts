/**
 * UserContextService — 호환 re-export
 * 본체는 @/shared/lib/userContextService로 이동됨.
 * @deprecated @/shared/lib/userContextService에서 직접 import하세요.
 */
export { UserContextService, uploadConfig, uploadHelpers } from '@/shared/lib/userContextService'
